// stage配置用のコマンドテキスト
// オブジェクトの種類を選択して座標等を指定する
// オブジェクト種類 : floor,wall,door,candle,start
// wallの時だけフラグの設定も必須
// 書式 : pos(x z) scale(sx sz) rot(ry) flag(1 or 0)
// pos,scaleのy軸成分はオブジェクトごとに固定なので不要
// rotのx,z成分は今のところは使わないので不要
// 配置例：wall pos(1 3) scale(5 2) rot(65) flag(0)
// wallのフラグで消える壁か普通の壁かを判断する

floor scale(10 1 10)
door pos(0 0 0) rot(90)
candle pos(5 0 5)
candle pos(-5 0 -5)
wall pos(0 0 0) scale(1 5 10) flag(1)
start pos(31 0 0)